hrs = float(input("Enter Hours:"))

rate = float(input("Enter rate:"))

if hrs > 40:
    overtimehrs = hrs - 40
    pay = 40 * rate  
    overtimepay = overtimehrs * (rate * 1.5)
    total = pay + overtimepay
else:
    total = hours * rate

print(total)
